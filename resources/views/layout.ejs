<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RealTImePizza</title>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>
    <nav class="container mx-auto flex items-center justify-between py-1">
            <div class="pt-3 ml-2"> 
                <a href="\"><img src="/img/DEEPAK KUMAR.png"  width='160' height='160' alt="logo"></a>
            </div>
        <div class="pr-8 pt-1">
            <ul class="flex items-center px-6 mb-2">
                <li class="ml-2 text-2xl"><a href='/' >Menu</a></li>
                <li class="ml-10 text-2xl"><a href="#">Offers</a></li>
                <% if(user) { %>
                <li class="ml-10 text-2xl">
                    <form id="logout" action="/logout"  method="POST">
                        <a onclick="document.getElementById('logout' ).submit()" 
                        href="#">Logout</a>
                    </form>
                </li>
                <% } else { %>
                    <li class="ml-10 text-2xl"><a href="/register">Register</a></li>
                <li class="ml-10 text-2xl"><a href="/login">Login</a></li>
                <% } %>
                <li class="ml-10"><a href="/cart" class="inline-block px-4 py-2 rounded-full flex items-center"> 
                    <span id="cartCounter" class="text-white font-bold pr-2"><%=session.cart ? session.cart.totalQty : ''%></span>
                    <img src="/img/shopping-cart.png" width="20" height="20"></a></li>
            </ul>
        </div>
    </nav>
    <%- body %>
    <script src="/js/app.js"></script>
</body>
</html>